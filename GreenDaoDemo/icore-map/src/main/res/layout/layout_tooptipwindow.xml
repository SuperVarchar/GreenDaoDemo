<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              xmlns:tools="http://schemas.android.com/tools"
              android:orientation="vertical">

    <ImageView
        android:id="@+id/tooltip_nav_up"
        android:layout_width="@dimen/base32dp"
        android:layout_height="@dimen/base32dp"
        android:layout_gravity="center"
        android:background="@drawable/tooltips_nav_up"
        />

    <TextView
        android:id="@+id/toolTip_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/tooltips_bg"
        android:gravity="center"
        android:padding="@dimen/base16dp"
        tools:text="我是PopopWindow的文字显示 :)"
        android:alpha="0.87"
        android:fontFamily="sans-serif"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        />


</LinearLayout>